<?php

/**
 * @var Laminas\View\Renderer\PhpRenderer $this
 *
 * Friendly 404 page. Like error/index.phtml, exception details are only
 * rendered when `view_manager.display_exceptions` is on (dev mode); in
 * production it shows a plain "page not found" message.
 */

$showDetails = !empty($this->display_exceptions) && isset($this->exception) && $this->exception instanceof \Throwable;
?>
<body class="am-splash-screen">
    <div class="am-wrapper am-error am-error-404">
        <div class="am-content">
            <div class="main-content">
                <div class="error-container">
                    <div class="error-image"></div>
                    <div class="error-number">
                        <h1><?= $this->translate('Page not found') ?></h1>
                        <h2><?= $this->escapeHtml($this->message ?? $this->translate('The page you requested could not be found.')) ?></h2>
                    </div>
                    <p style="margin-top: 1.5rem; color: #555;">
                        <?= $this->translate('Check the URL for typos, or use the link below to return home.') ?>
                    </p>
                    <p>
                        <a href="<?= $this->basePath() ?>" class="btn btn-primary">
                            <?= $this->translate('Return home') ?>
                        </a>
                    </p>

                    <?php if (!empty($this->display_not_found_reason) && !empty($this->reason)) : ?>
                        <hr />
                        <p><strong><?= $this->translate('Reason') ?>:</strong> <?= $this->escapeHtml($this->reason) ?></p>
                    <?php endif; ?>

                    <?php if ($showDetails) : ?>
                        <hr />
                        <h2><?= $this->translate('Developer details') ?></h2>
                        <h3><?= $this->escapeHtml(get_class($this->exception)) ?></h3>
                        <dl>
                            <dt><?= $this->translate('File') ?>:</dt>
                            <dd>
                                <pre><?= $this->escapeHtml($this->exception->getFile() . ':' . $this->exception->getLine()) ?></pre>
                            </dd>
                            <dt><?= $this->translate('Message') ?>:</dt>
                            <dd>
                                <pre><?= $this->escapeHtml($this->exception->getMessage()) ?></pre>
                            </dd>
                            <dt><?= $this->translate('Stack trace') ?>:</dt>
                            <dd>
                                <pre><?= $this->escapeHtml($this->exception->getTraceAsString()) ?></pre>
                            </dd>
                        </dl>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</body>
